---
- name: Implementing host scope variables for single server
  hosts: 172.31.83.244
  tasks:
    - name: User creation
      user:
        name: "{{a}}"
        password: "{{b}}"
        uid: "{{c}}"
        home: "{{d}}"
        comment: "{{e}}"
        shell: "{{f}}"
...          

